sudo: false
language: c++
compiler:
  - gcc
  - clang

    #addons:
    #  apt:
    #    sources:
    #    packages:
    #      - package-name-dev


script:
    - cd testing/
    - make run_tests verbose=1
    - ./run_tests


